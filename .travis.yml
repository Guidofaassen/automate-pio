---
sudo: required
language: java
before_install:
- export PATH=$PATH:/home/travis/PredictionIO/bin
- curl -s https://install.prediction.io/install.sh >install.sh
- bash install.sh -y
- pio-start-all
- yes n | pio template get PredictionIO/template-scala-parallel-complementarypurchase MyComplementaryPurchase --name fakeName --package testPackage --email None
- cd MyComplementaryPurchase
script:
- echo "done"
