---
sudo: required
language: scala
before_install:
- curl -s https://install.prediction.io/install.sh >install.sh
- bash -x install.sh -y
- ls -la /home/travis/PredictionIO
- /home/travis/PredictionIO/bin/pio-start-all
